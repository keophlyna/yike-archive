# Cline context filter — files/folders listed here are excluded from

# what Cline reads when scanning the repo. Same syntax as .gitignore.

# Dependencies — huge, never need editing

node_modules/

# Build output — regenerated, never hand-edited

.next/
out/
build/

# Lockfile — large, mechanical, rarely relevant to a prompt

package-lock.json

# Git internals

.git/

# Editor/OS noise

.vscode/
.DS_Store

# Large binary assets — Cline should reference these by path, not read their bytes

public/entries/_.jpg
public/entries/_.jpeg
public/entries/_.png
public/entries/_.gif
public/entries/\*.webp

# Env/secrets — should never be read into context

.env
.env.local
.env\*.local
